services:
  nginx:
    environment:
      NGINX_GENERATE_SELF_SIGNED_CERTIFICATE: "true"

  sqlserver:
    ports:
      - "127.0.0.1:1433:1433"

  redis:
    ports:
      - "127.0.0.1:6379:6379"

  rabbitmq:
    ports:
      - "127.0.0.1:5672:5672"
